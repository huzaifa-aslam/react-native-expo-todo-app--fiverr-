import { StatusBar } from 'expo-status-bar';
import { SafeAreaView, View } from 'react-native';
import MainRoute from './rotue/index'

export default function App() {
  return (
      <MainRoute/>
  );
}
